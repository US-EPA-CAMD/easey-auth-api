describe('-- Authentication Service --', () => {

  describe('* authenticate *', () => {
    it('should authenticate a user from service.', async () => {
      console.log('testing authenticate service.');
    });
  });
});
