describe('-- Credentials DTO --', () => {

  describe('* verify credentials dto props *', () => {
    it('should verify credentials dto properties.', async () => {
      console.log('testing credentials dto.');
    });
  });
});
